---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import DetailSidebar from '../../../components/DetailSidebar.astro';
import { getLocalizedCollection, getTranslation } from '../../../lib/content-data';

export function getStaticPaths() {
  return getLocalizedCollection('teachingPages', 'en').map((entry) => ({ params: { slug: entry.pathSlug }, props: { entry } }));
}

const { entry } = Astro.props;
const alt = getTranslation('teachingPages', entry.translationKey, 'it');
---
<BaseLayout locale="en" title={entry.seo.title} description={entry.seo.description} image="/images/people/federico-cerutti.png" alternatePath={alt ? `/teaching/${alt.pathSlug}/` : '/teaching/'}>
  <section class="section">
    <div class="container detail-shell">
      <article class="surface detail-article">
        <span class="eyebrow">{entry.typeLabel}</span>
        <h1 class="detail-title">{entry.title}</h1>
        <div class="detail-facts">
          <p class="muted"><strong>Date:</strong> {entry.dateLabel}</p>
          <p class="muted"><strong>Lecturers:</strong> {entry.people.join(', ')}</p>
          <p class="muted"><strong>Location:</strong> {entry.location}</p>
          <p class="muted"><a href={entry.sourceUrl} target="_blank" rel="noreferrer">Original page</a></p>
        </div>
        <div class="prose" set:html={entry.body} />
      </article>
      <DetailSidebar title="Topics" items={entry.tags} />
    </div>
  </section>

  <style>
    .prose iframe {
      width: 100%;
      max-width: 100%;
      aspect-ratio: 16 / 9;
      height: auto;
      border: 0;
      margin: 0.75rem 0 1.5rem;
    }
  </style>
</BaseLayout>
